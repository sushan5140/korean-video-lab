# Haneul X Ambassador-only tools — 2026-09-19

The private creator workspace is now a **separate page**: `/ambassadors/` (or `/ambassadors/?code=<SIGNATURE>`). The learner/community page `/creators/` has its own navigation, contains only challenges, video feeds, AI tutor, speaking practice and opt-in rankings, and does **not** render the BYOK key field, content factory, ambassador content library or creator challenge-publishing controls. The ambassador-only page includes the AI Factory and the moved Creator Studio (challenge/creator page management, Ready-video selection, caption-verified lesson submission, YouTube share drops and content library).

- **OpenRouter BYOK**: Ambassador supplies their own `sk-or-...` API key and selects or enters a model ID. Key is kept in JS/browser input only, forwarded to `/api/ambassador-ai` in a request header over HTTPS, and is never written into Haneul's Supabase tables, localStorage or URLs. Disconnect clears JS and input; a reload clears the key. Provider may charge ambassador according to their model and plan. Existing learner AI remains on the separate Groq-based `/api/creator-ai`. `api/ambassador-ai.js` verifies the user's Google session through Supabase `haneul_creator_owner` **before** forwarding to OpenRouter, includes timeout and high-level provider errors without echoing key or account information.
- **AI Content Factory**: One generation returns carousel copy, short Reel script, social caption and a Korean community challenge. Topic, language difficulty, platform and optional authentic Korean caption excerpt provided by creator. Output editable, copyable and saveable as private draft, then shareable with referred community. Human review before publishing.
- **Quiz Battle Builder**: Generates 5 Korean multiple-choice questions and answer explanations from a supplied actual Korean excerpt (or original topic-based examples if no excerpt). The AI now requests a five-question structured JSON quiz (three options per question, answer index and explanation). Ambassador can edit it and save as a draft before sharing. Members play five questions once per account; 10 points per correct answer (50 maximum), see explanations after submitting, and see an opt-in nickname-only ranking. Database verifies answers and prevents multiple scored attempts for the same quiz. Casual learning quiz, not a proctored examination; client access cannot prevent deliberate gaming by technically sophisticated users.
- **Community Learning Doctor**: Receives only 3 code-scoped aggregate dashboard counts (referral members, completed challenge days, speaking weeks) and current weekly prompt. With zero activity, returns a starter plan instead of making up learners' mistakes or drop-off statistics.
- **YouTube Community Drops**: Creator pastes a new YouTube watch URL, short URL, Shorts URL or video ID with title/note. Publishing saves the ID and title only in an RLS-protected code-owned post and immediately shows a shareable card with thumbnail and watch link to referred learners on the existing Videos tab. It does not require a transcript; the original separate interactive Video-to-Lesson submission continues to require captions and quality checks. No fake audio synchronization.
- **Library**: `haneul_ambassador_posts` with RLS; no direct anon/authenticated table grants; `haneul_ambassador_save_post`, `haneul_ambassador_list_posts`, `haneul_ambassador_set_published` verify creator or referral membership via existing code-based functions. Max 150 saved posts per creator and JSON size limit 16k characters. Drafts visible only to creator/admin; published kits/quizzes/videos visible to referral members only.

**Owner preview**: Existing private paused `HNL-X-OWNER-PREVIEW-SIG00` retains admin access, seven Ready challenge videos and demo creator lesson. The ambassador tab should be visible there after deployment.

**Testing status**: SQL migration `haneul_ambassador_creator_posts_and_video_shares` applied; GitHub API and frontend scripts syntax checked and DOM id binding checked. A real charged OpenRouter generation requires the ambassador to supply a valid key, and has not been live-tested with a real key. A signed-in browser owner/learner walkthrough is still needed to prove live usage. Do not claim verified cost, paid generation, or watched video.

## Interactive quiz-battle extension

Additional migration `haneul_ambassador_playable_quiz_scores_and_optin_ranking` adds RLS-protected `haneul_ambassador_quiz_attempts`, server-side `haneul_ambassador_quiz_submit` and `haneul_ambassador_quiz_ranking`. Migration `haneul_ambassador_hide_quiz_answers_until_attempt` strips correct answer indexes and explanations from published quiz bodies returned to non-owner members before their attempt; the RPC returns them afterward. No email or account identifiers are shown on the public ranking. SQL transaction smoke-tested a 5/5 score, duplicate submission returning the original score, and ranking shape; transaction rolled back without leaving test posts or attempts. Keep provider-generated Korean under creator human review. A model that cannot produce valid quiz JSON triggers a clear regenerate/change-model error, rather than silently publishing an unplayable contest.

Member YouTube shares now have a code- and video-scoped URL; opening it selects Videos and scrolls to the relevant card. Owner private preview opens the Ambassador AI Factory tab by default. `vercel.json` disables creator-page script caching to avoid stale UI after rollout. Newest static deployment status must be verified separately from committed GitHub code. Never infer successful paid BYOK requests from syntax/backend-only checks.

## Separate pages — 2026-09-19

- `/creators/` uses `creators/index.html` and lean `creators/studio.js` for referred learners. Existing member YouTube cards, published content kits, one-attempt quiz battle, tutor, lesson player and ranking remain. An assigned ambassador/admin sees a link to their private studio in the header; regular learners never see its privileged tab or key field.
- `/ambassadors/` uses `ambassadors/index.html` and `ambassadors/studio.js`. Private sign-in/ownership check uses existing Haneul Supabase OAuth and `haneul_creator_my_communities()` filtering `is_owner`, followed by `haneul_creator_space()` verifying `is_owner` before displaying the working area. Admin's existing owner-preview space is selected by default if available. Each ambassador manages only their assigned signature; links back to the matching member community preserve the code.
- Existing API endpoints `/api/ambassador-ai`, `/api/creator-ai`, RPCs and database tables are reused; no cross-project data migration or replacement domain. Main Haneul's Admin Panel and owner links now point to `/ambassadors/` separately from `/creators/`. Vercel no-cache headers include both workspace routes/assets.
- **Deploy verification requirement:** GitHub commit is not production; check official Vercel alias `/ambassadors/` returns HTTP 200 and both production JS bundles match updated source. End-to-end real OpenRouter key/Google-browser experience has not been exercised by the assistant.
