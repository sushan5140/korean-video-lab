async function tr(text){const u='https://translate.googleapis.com/translate_a/single?client=gtx&sl=ko&tl=en&dt=t&q='+encodeURIComponent(text);const r=await fetch(u,{headers:{'user-agent':'Mozilla/5.0'}});if(!r.ok)throw new Error('translate_'+r.status);const d=await r.json();return (d?.[0]||[]).map(x=>x?.[0]||'').join('').trim()}
module.exports=async function(req,res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Cache-Control','public, s-maxage=604800, stale-while-revalidate=2592000');if(req.method!=='POST')return res.status(405).json({ok:false,error:'method_not_allowed'});const texts=[...new Set((req.body?.texts||[]).map(x=>String(x||'').trim()).filter(Boolean))].slice(0,120),out={};let next=0;async function worker(){while(next<texts.length){const i=next++,t=texts[i];try{out[t]=await tr(t)}catch{out[t]=''}}}await Promise.all(Array.from({length:Math.min(8,texts.length)},worker));return res.status(200).json({ok:true,source:'google-translate',meanings:out})};